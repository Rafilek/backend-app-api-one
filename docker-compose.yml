version: "3"

services:
  app:
    user: root:root
    build:
      context: .
    ports:
      - "8600:8600"
    volumes:
      - ./app:/app
    command: >
        sh -c "python manage.py runserver 0.0.0.8600"
